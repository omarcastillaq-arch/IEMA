import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.grana2.ecgbroker',
  appName: 'ECG Broker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
